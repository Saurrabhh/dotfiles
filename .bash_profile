# =====================================================================
# 🐚 Saurrabhh's Bash Profile (.bash_profile)
# Sourced by login shells on startup (e.g. Git Bash on Windows).
# Ensures .bashrc is loaded for interactive shells.
# =====================================================================

if [ -f ~/.bashrc ]; then
    . ~/.bashrc
fi
